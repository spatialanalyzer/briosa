// <auto-generated />
// Generated from the reviewed Briosa command catalog. Do not edit by hand.
syntax = "proto3";

package briosa.sa.v2026_1_0529_7.v1alpha1;

import "briosa/core/v1alpha1/operation_outcomes.proto";
import "briosa/sa/v2026_1_0529_7/v1alpha1/values.proto";

option csharp_namespace = "Briosa.Sa.V2026_1_0529_7.V1Alpha1";

service CollectionOperations {
  // Constructs a named point at the center of an explicitly identified circle.
  rpc ConstructPointAtCircleCenter(ConstructPointAtCircleCenterRequest) returns (ConstructPointAtCircleCenterResult);
  // Constructs a named point at the midpoint of an explicitly identified line.
  rpc ConstructPointAtLineMidpoint(ConstructPointAtLineMidpointRequest) returns (ConstructPointAtLineMidpointResult);
  // Constructs a named point by fitting an explicit list of source points.
  rpc ConstructPointFitToPoints(ConstructPointFitToPointsRequest) returns (ConstructPointFitToPointsResult);
  // Constructs a named point group from an explicit list of source points.
  rpc ConstructPointGroupFromPointNameList(ConstructPointGroupFromPointNameListRequest) returns (ConstructPointGroupFromPointNameListResult);
  // Constructs a named point at explicit working-frame coordinates.
  rpc ConstructPointInWorkingCoordinates(ConstructPointInWorkingCoordinatesRequest) returns (ConstructPointInWorkingCoordinatesResult);
  // Copies an explicit list of collection objects into a destination collection.
  rpc CopyObjectsToCollection(CopyObjectsToCollectionRequest) returns (CopyObjectsToCollectionResult);
  // Deletes one explicitly named collection.
  rpc DeleteCollection(DeleteCollectionRequest) returns (DeleteCollectionResult);
  // Deletes an explicit list of named points.
  rpc DeletePoints(DeletePointsRequest) returns (DeletePointsResult);
  // Returns the number of collections currently present in the connected SpatialAnalyzer model.
  rpc GetCollectionCount(GetCollectionCountRequest) returns (GetCollectionCountResult);
  // Returns the collection name at an explicit SpatialAnalyzer collection index.
  rpc GetCollectionNameByIndex(GetCollectionNameByIndexRequest) returns (GetCollectionNameByIndexResult);
  // Returns the number of points in a named SpatialAnalyzer group.
  rpc GetPointCountInGroup(GetPointCountInGroupRequest) returns (GetPointCountInGroupResult);
  // Returns the group object identities currently present in a named collection.
  rpc ListGroupsInCollection(ListGroupsInCollectionRequest) returns (ListGroupsInCollectionResult);
  // Returns the point identities currently present in a named SpatialAnalyzer group.
  rpc ListPointsInGroup(ListPointsInGroupRequest) returns (ListPointsInGroupResult);
  // Moves an explicit list of collection objects into a destination collection.
  rpc MoveObjectsToCollection(MoveObjectsToCollectionRequest) returns (MoveObjectsToCollectionResult);
  // Renames one explicitly identified collection.
  rpc RenameCollection(RenameCollectionRequest) returns (RenameCollectionResult);
  // Renames an explicitly identified point, with reviewed collision behavior selected by the caller.
  rpc RenamePoint(RenamePointRequest) returns (RenamePointResult);
  // Selects an existing collection as the default or constructs it when it does not exist.
  rpc SetOrConstructDefaultCollection(SetOrConstructDefaultCollectionRequest) returns (SetOrConstructDefaultCollectionResult);
}

message ConstructPointAtCircleCenterRequest {
  // The exact collection/object identity of the source circle.
  CollectionObjectName circle = 1;
  // The exact collection, group, and target name for the point to construct.
  PointName point_name = 2;
}

message ConstructPointAtCircleCenterResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message ConstructPointAtLineMidpointRequest {
  // The exact collection/object identity of the source line.
  CollectionObjectName line = 1;
  // The exact collection, group, and target name for the point to construct.
  PointName point_name = 2;
}

message ConstructPointAtLineMidpointResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message ConstructPointFitToPointsRequest {
  // The exact collection, group, and target identities of the points to fit.
  PointNameList point_names = 1;
  // The exact collection, group, and target name for the fitted point.
  PointName resulting_point_name = 2;
}

message ConstructPointFitToPointsResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message ConstructPointGroupFromPointNameListRequest {
  // The exact collection, group, and target identities of the points to include.
  PointNameList point_names = 1;
  // The exact collection/object identity for the point group to construct.
  CollectionObjectName group_name = 2;
}

message ConstructPointGroupFromPointNameListResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message ConstructPointInWorkingCoordinatesRequest {
  // The exact collection, group, and target name for the point to construct.
  PointName point_name = 1;
  // All three coordinates in the active working frame. The generated zero-vector sample is not an omission default.
  Vector3 working_coordinates = 2;
}

message ConstructPointInWorkingCoordinatesResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message CopyObjectsToCollectionRequest {
  // The exact collection/object identities to copy.
  CollectionObjectNameList source_objects = 1;
  // The exact destination collection name.
  optional string destination_collection_name = 2;
}

message CopyObjectsToCollectionResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message DeleteCollectionRequest {
  // The exact collection name to delete.
  optional string collection_name = 1;
}

message DeleteCollectionResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message DeletePointsRequest {
  // The exact collection, group, and target identities of the points to delete.
  PointNameList point_names = 1;
}

message DeletePointsResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message GetCollectionCountRequest {}

message GetCollectionCountResult {
  // The number of collections at the time the command executes.
  optional int32 collection_count = 1;
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message GetCollectionNameByIndexRequest {
  // The explicit collection index passed to SpatialAnalyzer; Briosa does not infer a default.
  optional int32 collection_index = 1;
}

message GetCollectionNameByIndexResult {
  // The collection name returned for the requested index.
  optional string collection_name = 1;
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message GetPointCountInGroupRequest {
  // The exact collection/object identity of the point group to inspect.
  CollectionObjectName group = 1;
}

message GetPointCountInGroupResult {
  // The number of points in the group at command execution time.
  optional int32 point_count = 1;
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message ListGroupsInCollectionRequest {
  // The exact collection name whose groups will be listed.
  optional string collection_name = 1;
}

message ListGroupsInCollectionResult {
  // The ordered collection/object identities returned by the exact MP command.
  CollectionObjectNameList groups = 1;
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message ListPointsInGroupRequest {
  // The exact collection/object identity of the point group to inspect.
  CollectionObjectName group = 1;
}

message ListPointsInGroupResult {
  // The ordered point identities returned by the exact MP command.
  PointNameList points = 1;
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message MoveObjectsToCollectionRequest {
  // The exact collection/object identities to move.
  CollectionObjectNameList source_objects = 1;
  // The exact destination collection name.
  optional string destination_collection_name = 2;
}

message MoveObjectsToCollectionResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message RenameCollectionRequest {
  // The exact existing collection name to rename.
  optional string original_collection_name = 1;
  // The exact collection name to assign.
  optional string new_collection_name = 2;
}

message RenameCollectionResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message RenamePointRequest {
  // The exact existing collection, group, and target name to rename.
  PointName original_point_name = 1;
  // The exact collection, group, and target name to assign.
  PointName new_point_name = 2;
  // Whether SpatialAnalyzer may overwrite an existing point at the destination name. Omission explicitly sends the reviewed false default.
  optional bool overwrite_if_exists = 3;
}

message RenamePointResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}

message SetOrConstructDefaultCollectionRequest {
  // The exact collection name to select or construct.
  optional string collection_name = 1;
}

message SetOrConstructDefaultCollectionResult {
  // Explicit MP and result-only argument retrieval outcome.
  briosa.core.v1alpha1.MpExecutionDetails execution = 1000;
}
