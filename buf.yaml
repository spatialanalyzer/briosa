version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  # Catalog-reviewed names intentionally use domain services and typed results.
  # CommandCatalogValidator enforces their deterministic form.
  except:
    - SERVICE_SUFFIX
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
